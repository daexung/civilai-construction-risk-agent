# P5 보류 표본 1회 평가

- 실행일: 2026-09-25
- 실행 전 정답 커밋: `b9721bd` (`ground_truth.json`의 원문 대조 수정)
- 정답 상태: 잠정. `human_review`는 미완료이며 실무 검토 완료로 간주하지 않는다.
- 실행: `experiments/parser_bench/p5_eval.py` 1회. 원시 출력은 `experiments/parser_bench/results/p5_holdout/p5_report.json`에 보관하고 커밋하지 않는다.
- 환경: 저장소 `.venv`의 Python 3.11.9 원본 경로가 없어 저장소 내부에 같은 버전의 Python을 설치하고 기존 `.venv/Lib/site-packages`를 `PYTHONPATH`로 사용했다.

| 경로 | 전체 | 무작위 | 표적 |
|---|---:|---:|---:|
| 새 ODL 경로 | 166/190 (87.4%) | 61/82 | 105/108 |
| 현행 PyMuPDF 경로 | 105/190 (55.3%) | 41/82 | 64/108 |

G7은 **미충족**이다. 새 경로의 사실 통과율은 기준 95%보다 낮다. 채점기가 분류한 실패 24건은 잘못 붙음 22건, 수량 누락 1건, 행 라벨 누락 1건이다. 세 필수 조건(95% 이상, 수량 누락 0, 잘못 붙음 0)이 모두 거짓이다. 두 경로 모두 정답 표 22개를 찾았고, 무작위 쪽의 오탐과 누락 감시 경고는 없었다. 현행 경로가 통과하고 새 경로가 실패한 사실도 없었다.

| 표 | 새 경로 실패 | 관찰 |
|---|---:|---|
| `h330_t1` | 16/16 | 16개 규격이 하나의 행 라벨과 규격 칸에 쌓여 독립 행으로 분리되지 않았다. |
| `h642_t2` | 4/7 | 타일공·보통인부와 각 수량이 한 레코드에 함께 붙었다. 감산 조건 2개도 하나의 비고 레코드에 함께 붙었다. |
| `h299_t1` | 1/1 | 채점기 분류는 수량 누락이다. |
| `h584_t1` | 1/22 | ‘말박기 계산’ 행 라벨 누락으로 분류됐다. |
| `h868_t1` | 2/21 | `〃`가 있는 가용접·도장준비 행의 직종 승계가 정답과 일치하지 않았다. 도장준비의 원문 직종은 바로 윗행의 플랜트제관공이다. |

이 보류 표본은 한 번 사용됐으므로 이후 코드를 수정하고 다시 채점하면 새 보류 표본이 필요하다. 결과는 잠정 정답에 대한 평가이며, 실무 검토는 계속 미완료다. G7 실패로 기본 경로 전환은 진행하지 않는다.
