# 수정 후 남은 0레코드 표 10개

`results/summary.json`의 0레코드 표 목록에서 seed 2026으로 최대 10개를 무작위 추출했다. 남은 표가 정확히 10개라 모두 포함됐다. 아래 사유는 PDF에서 `find_tables().tables[i].extract()`로 읽힌 격자와 현재 파서 규칙을 대조한 것이다. 모두 **한 행짜리 격자**라 첫 행을 머리글로 처리한 뒤 본문 행이 없다. 이 목록의 표는 이번 수정에서 추가로 파싱하지 않았다.

| PDF 쪽 | table_id | 격자 내용과 0레코드 사유 |
|---:|---|---|
| 76 | `p76-t2` | `종 류 / 할증률(%)` 한 행만 검출. 머리글만 있다. |
| 443 | `p443-t1` | `예인시 / (빈 칸)` 한 행만 검출. 단독 표제 성격이며 본문이 없다. |
| 980 | `p980-t0` | `항 목`과 여러 설치 공종 목록이 **같은 한 행**에 있다. 내용이 있지만 첫 행 전체가 머리글로 처리된다. |
| 776 | `p776-t0` | `비 고`와 옥외 노출 공사 가산 10% 등 설명이 **같은 한 행**에 있다. 내용이 있지만 첫 행 전체가 머리글로 처리된다. |
| 436 | `p436-t0` | `비 고`와 인력 부설 기준표 설명이 **같은 한 행**에 있다. 내용이 있지만 첫 행 전체가 머리글로 처리된다. |
| 922 | `p922-t2` | `비 고`와 시공량 30% 감 적용 설명이 **같은 한 행**에 있다. 내용이 있지만 첫 행 전체가 머리글로 처리된다. |
| 233 | `p233-t0` | `현장조건 / E` 한 행만 검출. 머리글만 있다. |
| 198 | `p198-t0` | `구조물 / 적용면적(㎡)` 한 행만 검출. 앞뒤 표와 이어지는 머리글로 커버리지 검사에서 명시적으로 제외했다. |
| 523 | `p523-t0` | 빈 첫 칸과 계기비행시간·왕복운항시간 등 목록이 **같은 한 행**에 있다. 내용이 있지만 첫 행 전체가 머리글로 처리된다. |
| 70 | `p70-t0` | `현장작업조건 / 작업반장수` 한 행만 검출. 머리글만 있다. |

전후 비교: 전체 0레코드 표 **449 → 10개**, PDF 185~214쪽 **14 → 1개**. 검출 표 총수는 2,326개로 같고, 오류 쪽은 0개다. 185~214쪽에서 레코드가 있는 표는 70 → 83개다.
